<p id="notice"><%= notice %></p>

<h1>Constructionplans</h1>


<ul>

    <% @construction_plans.each do |constructionplan| %>

        <li><span style="font-size:20px;">  <%= link_to constructionplan.header, constructionplan %></span>

      <span style="font-size:5px"><%= link_to 'Bearbeiten', edit_constructionplan_path(constructionplan) %></span>
      <span style="font-size:5px"><%= link_to 'Löschen', constructionplan, method: :delete, data: { confirm: 'Are you sure?' } %></span></li>

    <% end %>
</ul>


<%= link_to 'Neue Seite zum Bau', new_constructionplan_path %>
